// Action generator, triggers reducer of same type

// MAP ACTIONS
const mapZoom = (zoom) => ({
  type: "zoomValue",
  zoom: zoom
});

//export const zoomValue = (zoomValue = {}) => {
// return (dispatch, getState) => {
//   });
// };
//};
